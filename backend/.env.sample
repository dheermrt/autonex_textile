# Server Configuration
PORT=4000
MODE=development

# normal,cron,mqtt      ----  choose any combo with commas
SERVER_TYPES=normal
# JWT Configuration
JWT_SECRET=wjhfg982hf2039cj9023c092
# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/autonex-dashboard
# Redis Configuration (for Socket.IO adapter)
REDIS_URI=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# CORS Configuration
CLIENT_ORIGIN_URLS=http://localhost:5173,http://localhost:3000
# MQTT Configuration (not needed for donear_3, but required by backend)
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=admin
MQTT_PASSWORD=admin
MQTT_CLIENT_ID=autonex-backend
# Revolution Counter 1 Configuration (not needed for donear_3, but required)
REVOLUTION_COUNTER_1={"CONNECTION_TIMEOUT_SECONDS":300,"TRAILING_WINDOW_AVG_MINUTES":5}